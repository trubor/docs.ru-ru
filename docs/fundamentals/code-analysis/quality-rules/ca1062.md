---
title: 'CA1062: проверка аргументов открытых методов (анализ кода)'
description: 'Дополнительные сведения о правиле анализа кода CA1062: проверка аргументов открытых методов'
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
- Validate arguments of public methods
helpviewer_keywords:
- CA1062
- ValidateArgumentsOfPublicMethods
author: gewarren
ms.author: gewarren
dev_langs:
- CSharp
- VB
ms.openlocfilehash: 4f0194c1ff7f82521f8295080b503729aa97e4ba
ms.sourcegitcommit: e301979e3049ce412d19b094c60ed95b316a8f8c
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 12/16/2020
ms.locfileid: "97593777"
---
# <a name="ca1062-validate-arguments-of-public-methods"></a><span data-ttu-id="be34f-103">CA1062. Проверьте аргументы или открытые методы</span><span class="sxs-lookup"><span data-stu-id="be34f-103">CA1062: Validate arguments of public methods</span></span>

| | <span data-ttu-id="be34f-104">Значение</span><span class="sxs-lookup"><span data-stu-id="be34f-104">Value</span></span> |
|-|-|
| <span data-ttu-id="be34f-105">**Идентификатор правила**</span><span class="sxs-lookup"><span data-stu-id="be34f-105">**Rule ID**</span></span> |<span data-ttu-id="be34f-106">CA1062</span><span class="sxs-lookup"><span data-stu-id="be34f-106">CA1062</span></span>|
| <span data-ttu-id="be34f-107">**Категория**</span><span class="sxs-lookup"><span data-stu-id="be34f-107">**Category**</span></span> |<span data-ttu-id="be34f-108">Microsoft. Design</span><span class="sxs-lookup"><span data-stu-id="be34f-108">Microsoft.Design</span></span>|
| <span data-ttu-id="be34f-109">**Исправление не критическое или не критическое**</span><span class="sxs-lookup"><span data-stu-id="be34f-109">**Fix is breaking or non-breaking**</span></span> |<span data-ttu-id="be34f-110">Не критическое</span><span class="sxs-lookup"><span data-stu-id="be34f-110">Non-breaking</span></span>|

## <a name="cause"></a><span data-ttu-id="be34f-111">Причина</span><span class="sxs-lookup"><span data-stu-id="be34f-111">Cause</span></span>

<span data-ttu-id="be34f-112">Внешний видимый метод разыменование одного из ссылочных аргументов без проверки того, является ли этот аргумент `null` ( `Nothing` в Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="be34f-112">An externally visible method dereferences one of its reference arguments without verifying whether that argument is `null` (`Nothing` in Visual Basic).</span></span>

<span data-ttu-id="be34f-113">Следующие аспекты этого правила можно [настроить](#configure-code-to-analyze).</span><span class="sxs-lookup"><span data-stu-id="be34f-113">The following aspects of this rule are [configurable](#configure-code-to-analyze):</span></span>

- <span data-ttu-id="be34f-114">Следует ли анализировать параметр метода расширения this.</span><span class="sxs-lookup"><span data-stu-id="be34f-114">Whether or not to analyze extension method 'this' parameter.</span></span>
- <span data-ttu-id="be34f-115">Укажите методы проверки NULL в упоминаемых библиотеках или проектах, которые проверяют, что переданные аргументы не равны NULL.</span><span class="sxs-lookup"><span data-stu-id="be34f-115">Specify null check validation methods in referenced libraries or projects, which validate that arguments passed to it are non-null.</span></span>
- <span data-ttu-id="be34f-116">Части базы кода, которые необходимо исключить из анализа.</span><span class="sxs-lookup"><span data-stu-id="be34f-116">Parts of codebase to exclude from analysis.</span></span>

## <a name="rule-description"></a><span data-ttu-id="be34f-117">Описание правила</span><span class="sxs-lookup"><span data-stu-id="be34f-117">Rule description</span></span>

<span data-ttu-id="be34f-118">Все ссылочные аргументы, которые передаются в методы, видимые извне, должны проверяться по отношению к `null` .</span><span class="sxs-lookup"><span data-stu-id="be34f-118">All reference arguments that are passed to externally visible methods should be checked against `null`.</span></span> <span data-ttu-id="be34f-119">При необходимости вызовите исключение, если <xref:System.ArgumentNullException> аргумент имеет значение `null` .</span><span class="sxs-lookup"><span data-stu-id="be34f-119">If appropriate, throw a <xref:System.ArgumentNullException> when the argument is `null`.</span></span>

<span data-ttu-id="be34f-120">Если метод может быть вызван из неизвестной сборки, так как он объявлен как открытый или защищенный, следует проверить все параметры метода.</span><span class="sxs-lookup"><span data-stu-id="be34f-120">If a method can be called from an unknown assembly because it is declared public or protected, you should validate all parameters of the method.</span></span> <span data-ttu-id="be34f-121">Если метод предназначен для вызова только известными сборками, необходимо сделать метод внутренним и применить <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> атрибут к сборке, содержащей метод.</span><span class="sxs-lookup"><span data-stu-id="be34f-121">If the method is designed to be called only by known assemblies, you should make the method internal and apply the <xref:System.Runtime.CompilerServices.InternalsVisibleToAttribute> attribute to the assembly that contains the method.</span></span>

## <a name="how-to-fix-violations"></a><span data-ttu-id="be34f-122">Устранение нарушений</span><span class="sxs-lookup"><span data-stu-id="be34f-122">How to fix violations</span></span>

<span data-ttu-id="be34f-123">Чтобы устранить нарушение этого правила, проверьте каждый аргумент ссылки на `null` .</span><span class="sxs-lookup"><span data-stu-id="be34f-123">To fix a violation of this rule, validate each reference argument against `null`.</span></span>

## <a name="when-to-suppress-warnings"></a><span data-ttu-id="be34f-124">Когда следует подавлять предупреждения</span><span class="sxs-lookup"><span data-stu-id="be34f-124">When to suppress warnings</span></span>

<span data-ttu-id="be34f-125">Предупреждение из этого правила можно отключить, если вы уверены, что параметр разыменования был проверен другим вызовом метода в функции.</span><span class="sxs-lookup"><span data-stu-id="be34f-125">You can suppress a warning from this rule if you are sure that the dereferenced parameter has been validated by another method call in the function.</span></span>

## <a name="configure-code-to-analyze"></a><span data-ttu-id="be34f-126">Настройка анализируемого кода</span><span class="sxs-lookup"><span data-stu-id="be34f-126">Configure code to analyze</span></span>

<span data-ttu-id="be34f-127">Используйте следующие параметры, чтобы настроить, на какие части базы кода следует запускать это правило.</span><span class="sxs-lookup"><span data-stu-id="be34f-127">Use the following options to configure which parts of your codebase to run this rule on.</span></span>

- [<span data-ttu-id="be34f-128">Исключить конкретные символы</span><span class="sxs-lookup"><span data-stu-id="be34f-128">Exclude specific symbols</span></span>](#exclude-specific-symbols)
- [<span data-ttu-id="be34f-129">Исключить определенные типы и их производные типы</span><span class="sxs-lookup"><span data-stu-id="be34f-129">Exclude specific types and their derived types</span></span>](#exclude-specific-types-and-their-derived-types)
- [<span data-ttu-id="be34f-130">Исключить параметр метода расширения "this"</span><span class="sxs-lookup"><span data-stu-id="be34f-130">Exclude extension method 'this' parameter</span></span>](#exclude-extension-method-this-parameter)
- [<span data-ttu-id="be34f-131">Методы проверки значений NULL</span><span class="sxs-lookup"><span data-stu-id="be34f-131">Null check validation methods</span></span>](#null-check-validation-methods)

<span data-ttu-id="be34f-132">Эти параметры можно настроить только для этого правила, для всех правил или для всех правил в этой категории (конструктор).</span><span class="sxs-lookup"><span data-stu-id="be34f-132">These options can be configured for just this rule, for all rules, or for all rules in this category (Design).</span></span> <span data-ttu-id="be34f-133">Дополнительные сведения см. в разделе [Параметры конфигурации правила качества кода](../code-quality-rule-options.md).</span><span class="sxs-lookup"><span data-stu-id="be34f-133">For more information, see [Code quality rule configuration options](../code-quality-rule-options.md).</span></span>

[!INCLUDE[excluded-symbol-names](~/includes/code-analysis/excluded-symbol-names.md)]

[!INCLUDE[excluded-type-names-with-derived-types](~/includes/code-analysis/excluded-type-names-with-derived-types.md)]

### <a name="exclude-extension-method-this-parameter"></a><span data-ttu-id="be34f-134">Исключить параметр метода расширения "this"</span><span class="sxs-lookup"><span data-stu-id="be34f-134">Exclude extension method 'this' parameter</span></span>

<span data-ttu-id="be34f-135">По умолчанию это правило анализирует и помечает `this` параметр для методов расширения.</span><span class="sxs-lookup"><span data-stu-id="be34f-135">By default, this rule analyzes and flags the `this` parameter for extension methods.</span></span> <span data-ttu-id="be34f-136">Вы можете исключить анализ `this` параметра для методов расширения, добавив следующую пару "ключ-значение" в *editorconfig* -файл в проекте:</span><span class="sxs-lookup"><span data-stu-id="be34f-136">You can exclude analysis of the `this` parameter for extension methods by adding the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.exclude_extension_method_this_parameter = true
```

### <a name="null-check-validation-methods"></a><span data-ttu-id="be34f-137">Методы проверки значений NULL</span><span class="sxs-lookup"><span data-stu-id="be34f-137">Null check validation methods</span></span>

<span data-ttu-id="be34f-138">Это правило может привести к ложным срабатываниям, если код вызывает специальные методы проверки значений NULL в библиотеках или проектах.</span><span class="sxs-lookup"><span data-stu-id="be34f-138">This rule can lead to false positives if your code calls special null check validation methods in referenced libraries or projects.</span></span> <span data-ttu-id="be34f-139">Вы можете избежать этих ложных срабатываний, указав имя или сигнатуру методов проверки NULL.</span><span class="sxs-lookup"><span data-stu-id="be34f-139">You can avoid these false positives by specifying the name or signature of null check validation methods.</span></span>  <span data-ttu-id="be34f-140">Затем анализ предполагает, что после вызова аргументы, переданные этому методу, не равны NULL.</span><span class="sxs-lookup"><span data-stu-id="be34f-140">The analysis will then assume that arguments passed to this method are non-null after the call.</span></span> <span data-ttu-id="be34f-141">Например, чтобы пометить все методы с именами `Validate` как методы проверки со значением NULL, можно добавить следующую пару "ключ-значение" в *editorconfig* -файл в проекте:</span><span class="sxs-lookup"><span data-stu-id="be34f-141">For example, to mark all methods named `Validate` as null check validation methods, you can add the following key-value pair to an *.editorconfig* file in your project:</span></span>

```ini
dotnet_code_quality.CA1062.null_check_validation_methods = Validate
```

<span data-ttu-id="be34f-142">Допустимые форматы имени метода в значении параметра (разделенные `|` ):</span><span class="sxs-lookup"><span data-stu-id="be34f-142">Allowed method name formats in the option value (separated by `|`):</span></span>

- <span data-ttu-id="be34f-143">Только имя метода (включает все методы с именем, независимо от содержащего его типа или пространства имен).</span><span class="sxs-lookup"><span data-stu-id="be34f-143">Method name only (includes all methods with the name, regardless of the containing type or namespace)</span></span>
- <span data-ttu-id="be34f-144">Полные имена в [формате идентификатора документации](../../../csharp/programming-guide/xmldoc/processing-the-xml-file.md#id-strings)символа с дополнительным `M:` префиксом.</span><span class="sxs-lookup"><span data-stu-id="be34f-144">Fully qualified names in the symbol's [documentation ID format](../../../csharp/programming-guide/xmldoc/processing-the-xml-file.md#id-strings), with an optional `M:` prefix.</span></span>

<span data-ttu-id="be34f-145">Примеры:</span><span class="sxs-lookup"><span data-stu-id="be34f-145">Examples:</span></span>

| <span data-ttu-id="be34f-146">Значение параметра</span><span class="sxs-lookup"><span data-stu-id="be34f-146">Option Value</span></span> | <span data-ttu-id="be34f-147">Итоги</span><span class="sxs-lookup"><span data-stu-id="be34f-147">Summary</span></span> |
| --- | --- |
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate` | <span data-ttu-id="be34f-148">Соответствует всем методам `Validate` , названным в компиляции</span><span class="sxs-lookup"><span data-stu-id="be34f-148">Matches all methods named `Validate` in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = Validate1|Validate2` | <span data-ttu-id="be34f-149">Соответствует всем методам с именем либо `Validate1` `Validate2` в компиляции</span><span class="sxs-lookup"><span data-stu-id="be34f-149">Matches all methods named either `Validate1` or `Validate2` in the compilation</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS.MyType.Validate(ParamType)` | <span data-ttu-id="be34f-150">Соответствует конкретному методу `Validate` с заданной полной сигнатурой</span><span class="sxs-lookup"><span data-stu-id="be34f-150">Matches specific method `Validate` with given fully qualified signature</span></span>
|`dotnet_code_quality.CA1062.null_check_validation_methods = NS1.MyType1.Validate1(ParamType)|NS2.MyType2.Validate2(ParamType)` | <span data-ttu-id="be34f-151">Соответствует отдельным методам `Validate1` и `Validate2` соответствующим полным сигнатурам</span><span class="sxs-lookup"><span data-stu-id="be34f-151">Matches specific methods `Validate1` and `Validate2` with respective fully qualified signature</span></span>

## <a name="example-1"></a><span data-ttu-id="be34f-152">Пример 1</span><span class="sxs-lookup"><span data-stu-id="be34f-152">Example 1</span></span>

<span data-ttu-id="be34f-153">В следующем примере показан метод, нарушающий правило, и метод, который удовлетворяет правилу.</span><span class="sxs-lookup"><span data-stu-id="be34f-153">The following example shows a method that violates the rule and a method that satisfies the rule.</span></span>

```csharp
using System;

namespace DesignLibrary
{
    public class Test
    {
        // This method violates the rule.
        public void DoNotValidate(string input)
        {
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }

        // This method satisfies the rule.
        public void Validate(string input)
        {
            if (input == null)
            {
                throw new ArgumentNullException(nameof(input));
            }
            if (input.Length != 0)
            {
                Console.WriteLine(input);
            }
        }
    }
}
```

```vb
Imports System

Namespace DesignLibrary

    Public Class Test

        ' This method violates the rule.
        Sub DoNotValidate(ByVal input As String)

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

        ' This method satisfies the rule.
        Sub Validate(ByVal input As String)

            If input Is Nothing Then
                Throw New ArgumentNullException(NameOf(input))
            End If

            If input.Length <> 0 Then
                Console.WriteLine(input)
            End If

        End Sub

    End Class

End Namespace
```

## <a name="example-2"></a><span data-ttu-id="be34f-154">Пример 2</span><span class="sxs-lookup"><span data-stu-id="be34f-154">Example 2</span></span>

<span data-ttu-id="be34f-155">Конструкторы копий, заполняющие поля или свойства, являющиеся ссылочными объектами, также могут нарушать правило CA1062.</span><span class="sxs-lookup"><span data-stu-id="be34f-155">Copy constructors that populate field or properties that are reference objects can also violate the CA1062 rule.</span></span> <span data-ttu-id="be34f-156">Нарушение возникает из-за того, что копируемый объект, передаваемый в конструктор копирования, может быть `null` ( `Nothing` в Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="be34f-156">The violation occurs because the copied object that is passed to the copy constructor might be `null` (`Nothing` in Visual Basic).</span></span> <span data-ttu-id="be34f-157">Чтобы устранить нарушение, используйте статический метод (Shared в Visual Basic), чтобы убедиться, что скопированный объект не имеет значение null.</span><span class="sxs-lookup"><span data-stu-id="be34f-157">To resolve the violation, use a static (Shared in Visual Basic) method to check that the copied object is not null.</span></span>

<span data-ttu-id="be34f-158">В следующем `Person` примере класса `other` объект, который передается в `Person` конструктор копии, может иметь значение `null` .</span><span class="sxs-lookup"><span data-stu-id="be34f-158">In the following `Person` class example, the `other` object that is passed to the `Person` copy constructor might be `null`.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor CA1062 fires because other is dereferenced
    // without being checked for null
    public Person(Person other)
        : this(other.Name, other.Age)
    {
    }
}
```

## <a name="example-3"></a><span data-ttu-id="be34f-159">Пример 3</span><span class="sxs-lookup"><span data-stu-id="be34f-159">Example 3</span></span>

<span data-ttu-id="be34f-160">В следующем исправленном `Person` примере `other` объект, который передается в конструктор копии, сначала проверяется на значение NULL в `PassThroughNonNull` методе.</span><span class="sxs-lookup"><span data-stu-id="be34f-160">In the following revised `Person` example, the `other` object that is passed to the copy constructor is first checked for null in the `PassThroughNonNull` method.</span></span>

```csharp
public class Person
{
    public string Name { get; private set; }
    public int Age { get; private set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }

    // Copy constructor
    public Person(Person other)
        : this(PassThroughNonNull(other).Name, other.Age)
    {
    }

    // Null check method
    private static Person PassThroughNonNull(Person person)
    {
        if (person == null)
            throw new ArgumentNullException(nameof(person));
        return person;
    }
}
```
